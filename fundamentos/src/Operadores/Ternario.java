package operadores;

public class Ternario {
    public static void main(String[] args) {
        // Declaração de variáveis
       double media = 7.6;
         String resultado = media >= 5.0 ? "em recuperacao" : "aprovado";

         System.out.println("O aluno esta  " + resultado);
    }
}
